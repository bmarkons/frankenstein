<p id="notice"><%= notice %></p>

<h1>Comments</h1>

<table>
  <thead>
    <tr>
      <th>Grade</th>
      <th>Text</th>
      <th>Accommodation</th>
      <th>Posted by</th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.grade %></td>
        <td><%= comment.text %></td>
        <td><%= comment.accommodation %></td>
        <td><%= comment.user.email %></td>
        <td><%= link_to 'Edit', edit_comment_path(comment) if comment.posted_by?(current_user) %></td>
        <% if comment.posted_by?(current_user) || current_user.type == "admin" %>
          <td><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<% if @accommodation.present? && !@accommodation.commented_by?(current_user) && current_user.type == "user" %>
  <%= link_to 'New Comment', new_accommodation_comment_path if current_user.type == "user" && @accommodation %>
<% end %>
