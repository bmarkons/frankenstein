<p id="notice"><%= notice %></p>

<h1>Room Reservations</h1>

<table>
  <thead>
    <tr>
      <th>Start date</th>
      <th>End date</th>
      <th>Room</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @room_reservations.each do |room_reservation| %>
      <tr>
        <td><%= room_reservation.start_date %></td>
        <td><%= room_reservation.end_date %></td>
        <td><%= room_reservation.room.name %></td>
        <td><%= link_to 'Show', room_reservation %></td>
        <td><%= link_to 'Destroy', room_reservation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
